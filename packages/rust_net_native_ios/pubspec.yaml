name: rust_net_native_ios
description: iOS native carrier package for rust_net artifacts and build integration.
version: 2.0.0
environment:
  sdk: ^3.11.0
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter
  code_assets: ^1.0.0
  hooks: ^1.0.2

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.3
