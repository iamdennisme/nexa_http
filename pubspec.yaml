name: nexa_http_workspace
description: Workspace for nexa_http packages.
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  nexa_http_native_runtime_internal:
    path: packages/nexa_http_native_runtime_internal

dev_dependencies:
  code_assets: ^1.0.0
  crypto: ^3.0.6
  hooks: ^1.0.2
  path: ^1.9.0
  test: ^1.26.3
  yaml: ^3.1.3
