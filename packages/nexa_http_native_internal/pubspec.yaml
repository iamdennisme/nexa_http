name: nexa_http_native_internal
description: Internal target, artifact, and native bindings helper for nexa_http.
version: 2.0.2
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  crypto: ^3.0.6
  ffi: ^2.2.0
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  ffigen: ^20.1.1
  test: ^1.26.3
